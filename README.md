# Delulu

The sunny side of the internet.

## Setup

```bash
# install dependencies
yarn install

# Start the development server
yarn dev

# Build the application for production
yarn build

# Locally preview production build
yarn preview
```
